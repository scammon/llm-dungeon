# The Hollow Deep — web backend package.
# Shared pure game logic (data, config, gen, player, combat, meta) lives at the
# repo root and is imported as top-level modules; this package adds the web
# layer (engine, narrator, db, sessions, api, main).
